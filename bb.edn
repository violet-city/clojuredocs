{:tasks {start-mongo    (shell "docker compose up -d mongo")
         start-postgres (shell "docker compose up -d postgres")
         start-cljs     (shell "npx shadow-cljs server")
         dev            {:depends [start-mongo start-cljs]}}}
